import { TChapterPassage } from 'types/TPassage';
import { TWorldState } from 'data/TWorldState';
import { Engine } from 'code/Engine/ts/Engine';
{characterImports}

export type {chapterPassageIdType} = {passageIdUnion};

{characterPassageIdTypes}

const {chapterPassagesConstName}: Record<{chapterPassageIdType}, (s: TWorldState, e: Engine) => TChapterPassage<{quotedChapterId}>> = {
{passageRecordEntries}
};

export default {chapterPassagesConstName};
import { TChapter } from 'types/TChapter';
import { Time } from 'time/Time';

export const {mainChapterFunction}: TChapter<{quotedChapterId}> = {
    chapterId: {quotedChapterId},
    title: '{chapterTitle}',
    description: '{chapterDescription}',
    timeRange: {
        start: Time.fromString({quotedTimeStart}),
        end: Time.fromString({quotedTimeEnd}),
    },
    location: {quotedLocation},

    children: [],

    triggers: [],

    init: {
        {initObjectContent}
    },
};

export type {chapterDataTypeName} = {
    {chapterDataTypeContent}
};